name: "Webhook Event"

on:
  repository_dispatch:
    types:
      - custom_webhook

jobs:
  run_script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Run a script
        run: echo "Event of type: $GITHUB_EVENT_NAME"
